# Dynamic Invoker Email Service

## Purpose
The Dynamic Invoker Email Service provides an implementation of a dynamic invoker that sends emails. It can be used as an action in scenarios (Rules Model).
