package com.wizzdi.basic.iot.model;

public enum HealthSignalSourceType {
    STATE_PROPERTY,
    REMOTE_CONNECTIVITY,
    REMOTE_LAST_SEEN_AGE_SECONDS,
    REMOTE_VERSION,
    REMOTE_CURRENT_SEVERITY,
    REMOTE_HUMAN_INTERVENTION
}
